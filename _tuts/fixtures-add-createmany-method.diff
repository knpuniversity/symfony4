diff --git a/src/DataFixtures/AppFixture.php b/src/DataFixtures/AppFixture.php
index 525be39..0512573 100644
--- a/src/DataFixtures/AppFixture.php
+++ b/src/DataFixtures/AppFixture.php
@@ -4,15 +4,16 @@ namespace App\DataFixtures;
 
 use App\Entity\Article;
 use Doctrine\Common\Persistence\ObjectManager;
+use Doctrine\ORM\EntityManagerInterface;
 
 class AppFixture extends BaseFixture
 {
     public function loadData(ObjectManager $manager)
     {
-        $article = new Article();
-        $article->setTitle('Why Asteroids Taste Like Bacon');
-        $article->setSlug('why-asteroids-taste-like-bacon-'.rand(100, 999));
-        $article->setContent(<<<EOF
+        $this->createMany(Article::class, 10, function(Article $article, $count) {
+            $article->setTitle('Why Asteroids Taste Like Bacon');
+            $article->setSlug('why-asteroids-taste-like-bacon-'.$count);
+            $article->setContent(<<<EOF
 Spicy **jalapeno bacon** ipsum dolor amet veniam shank in dolore. Ham hock nisi landjaeger cow,
 lorem proident [beef ribs](https://baconipsum.com/) aute enim veniam ut cillum pork chuck picanha. Dolore reprehenderit
 labore minim pork belly spare ribs cupim short loin in. Elit exercitation eiusmod dolore cow
@@ -30,17 +31,16 @@ strip steak pork belly aliquip capicola officia. Labore deserunt esse chicken lo
 cow est ribeye adipisicing. Pig hamburger pork belly enim. Do porchetta minim capicola irure pancetta chuck
 fugiat.
 EOF
-        );
+            );
 
-        // publish most articles
-        if (rand(1, 10) > 2) {
-            $article->setPublishedAt(new \DateTime(sprintf('-%d days', rand(1, 100))));
-        }
+            // publish most articles
+            if (rand(1, 10) > 2) {
+                $article->setPublishedAt(new \DateTime(sprintf('-%d days', rand(1, 100))));
+            }
 
-        $article->setAuthor('Mike Ferengi');
-        $article->setHeartCount(rand(5, 100));
-
-        $manager->persist($article);
+            $article->setAuthor('Mike Ferengi');
+            $article->setHeartCount(rand(5, 100));
+        });
 
         $manager->flush();
     }
diff --git a/src/DataFixtures/BaseFixture.php b/src/DataFixtures/BaseFixture.php
index c3216f5..31b93e2 100644
--- a/src/DataFixtures/BaseFixture.php
+++ b/src/DataFixtures/BaseFixture.php
@@ -18,4 +18,16 @@ abstract class BaseFixture extends Fixture
 
         $this->loadData($manager);
     }
+
+    protected function createMany(string $className, int $count, callable $factory)
+    {
+        for ($i = 0; $i < $count; $i++) {
+            $entity = new $className();
+            $factory($entity, $i);
+
+            $this->manager->persist($entity);
+            // store for usage later as App\Entity\ClassName_#COUNT#
+            $this->addReference($className . '_' . $i, $entity);
+        }
+    }
 }
