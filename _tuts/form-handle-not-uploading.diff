diff --git a/src/Controller/ArticleAdminController.php b/src/Controller/ArticleAdminController.php
index 404021d..2bfb5f7 100644
--- a/src/Controller/ArticleAdminController.php
+++ b/src/Controller/ArticleAdminController.php
@@ -56,16 +56,18 @@ class ArticleAdminController extends BaseController
         if ($form->isSubmitted() && $form->isValid()) {
             /** @var UploadedFile $uploadedFile */
             $uploadedFile = $form['imageFile']->getData();
-            $destination = $this->getParameter('kernel.project_dir').'/public/uploads/article_image';
-
-            $originalFilename = pathinfo($uploadedFile->getClientOriginalName(), PATHINFO_FILENAME);
-            $newFilename = Urlizer::urlize($originalFilename).'-'.uniqid().'.'.$uploadedFile->guessExtension();
-
-            $uploadedFile->move(
-                $destination,
-                $newFilename
-            );
-            $article->setImageFilename($newFilename);
+            if ($uploadedFile) {
+                $destination = $this->getParameter('kernel.project_dir').'/public/uploads/article_image';
+
+                $originalFilename = pathinfo($uploadedFile->getClientOriginalName(), PATHINFO_FILENAME);
+                $newFilename = Urlizer::urlize($originalFilename).'-'.uniqid().'.'.$uploadedFile->guessExtension();
+
+                $uploadedFile->move(
+                    $destination,
+                    $newFilename
+                );
+                $article->setImageFilename($newFilename);
+            }
 
             $em->persist($article);
             $em->flush();
diff --git a/src/Form/ArticleFormType.php b/src/Form/ArticleFormType.php
index 2297b7e..a933538 100644
--- a/src/Form/ArticleFormType.php
+++ b/src/Form/ArticleFormType.php
@@ -52,7 +52,8 @@ class ArticleFormType extends AbstractType
                 'required' => false,
             ])
             ->add('imageFile', FileType::class, [
-                'mapped' => false
+                'mapped' => false,
+                'required' => false,
             ])
         ;
 
