diff --git a/tests/MailerTest.php b/tests/MailerTest.php
index 7f795e90..7201660e 100644
--- a/tests/MailerTest.php
+++ b/tests/MailerTest.php
@@ -8,6 +8,7 @@ use Knp\Snappy\Pdf;
 use PHPUnit\Framework\TestCase;
 use Symfony\Bundle\FrameworkBundle\Test\WebTestCase;
 use Symfony\Component\Mailer\MailerInterface;
+use Symfony\Component\Mime\NamedAddress;
 use Twig\Environment;
 
 class MailerTest extends WebTestCase
@@ -24,6 +25,14 @@ class MailerTest extends WebTestCase
         $user->setEmail('victor@symfonycasts.com');
 
         $mailer = new Mailer($symfonyMailer, $pdf, $twig);
-        $mailer->sendWelcomeMessage($user);
+        $email = $mailer->sendWelcomeMessage($user);
+
+        $this->assertEquals('Welcome to the Space Bar!', $email->getSubject());
+        $this->assertCount(1, $email->getTo());
+        /** @var NamedAddress[] $namedAddresses */
+        $namedAddresses = $email->getTo();
+        $this->assertInstanceOf(NamedAddress::class, $namedAddresses[0]);
+        $this->assertEquals('Victor', $namedAddresses[0]->getName());
+        $this->assertEquals('victor@symfonycasts.com', $namedAddresses[0]->getAddress());
     }
 }
